# NanoFIT 实施计划

> 这份文档是整个项目的施工图，2026-09-23 开工前经项目主人确认。
> 里面每个术语第一次出现时都用大白话解释过，不用有编程基础也能看懂。
>
> **当前进度**：阶段 1～3 已完成 ☑ ／ 阶段 4～6 未开始 ☐
> 每完成一个阶段，我都会回来更新这一行。

---

## 〇、先说三件我必须提醒你的事

### 1. 你有一处要求自己打自己

你列的功能有 **7 项**（动作库、记训练、计时器、历史、统计、模板、身体数据），但阶段划分只有 **6 个阶段**，而且功能第 7 项"身体数据"在这 6 个阶段里**没有位置**。

**我的处理**：把"身体数据"放进**阶段 4**（和"历史+模板"一起）。因为这三样都属于"看和管理已经记下来的东西"，放一起最自然。

**另外**：动作库、模板管理、身体数据这三个功能，在 6 个阶段的标题里都没有出现。它们会作为"**设置**"这个 tab 里的三个入口存在（这是你刚才选的方案）。

### 2. 你的两份文档路径对不上

`Nanofit设计\Claude-Code-提示词.md` 里写的命令是复制 `E:\Vibe Coding\Nano健身\...`，但实际文件夹叫 **`Nanofit`** 不是 `Nano健身`。我按实际路径来。

### 3. 你的"规矩本"原本没生效（阶段 1 已处理）

`CLAUDE.md`（里面写着技术栈、数据模型、配色、公式那份）原来躺在 `Nanofit设计` 文件夹里。**Claude Code 只读工作目录下的 CLAUDE.md**，所以它是一条睡着的规矩。

**阶段 1 已把它复制到 `Nanofit工程\CLAUDE.md`**，现在生效了。`Nanofit设计` 里那份原件没动，留着当备份。

---

## 一、这个 App 是什么（一句话）

一个**只装在你自己手机上、断网也能用、数据存在你自己手机里**的健身记录本。没有账号，没有服务器，你的训练数据永远不会上传到任何地方。

**这些词先解释一下，后面会反复出现：**

| 术语 | 大白话 |
|---|---|
| **Vite**（读"维特"） | 一个"项目脚手架"。空文件夹交给它，它自动生成一套能跑起来的基础文件，你只要往里填功能 |
| **React**（读"瑞艾克特"） | 把界面拆成一块块"积木"，每块积木自己管自己。改哪块只动哪块 |
| **TypeScript** | 写代码时自动帮你抓错别字和类型搞混的工具。比普通 JavaScript 多一层保险 |
| **Tailwind CSS** | 不用另写样式文件，直接在标签上写 `bg-surface` 这种短名字就能上颜色、调间距 |
| **recharts** | 画图表的零件库（折线图、柱状图） |
| **localStorage** | 浏览器自带的"储物柜"，按网址分开存东西。你手机上的训练数据就放这儿。**清缓存会清掉它** |
| **PWA** | 一项让网页能"装到手机桌面、点开像真 App、没地址栏、断网也能打开"的技术 |
| **Service Worker** | PWA 的实现方式，可以理解成一个"离线仓库管理员"：第一次打开时把文件都存下来，之后断网也能拿出来用 |
| **JSON** | 一种通用文本格式，用来把数据存成文件。导出备份就是导出一个 .json 文件 |
| **git commit** | 游戏存档。每次做完一个阶段存一次档，搞砸了能读档回来 |

---

## 二、你已经拍板的 4 个决定

| 决定 | 你选的 | 意思 |
|---|---|---|
| 7 个功能怎么装进 4 个 tab | **「设置」当工具箱** | 训练 tab 只管记训练；动作库 / 模板 / 身体数据 / 备份，都进"设置"里点第二下进去 |
| Tailwind 版本 | **v4 新版** | 少两个配置文件，少一个"样式突然全失效"的经典坑 |
| 页面切换方式 | **不装路由** | 用一个变量记住"现在在哪个 tab"，代码最短最好懂 |
| 身高怎么记 | **也按日期记** | 每次量身体数据时填身高，能画趋势图 |

> ⚠️ 第 4 条会让你原来的 `CLAUDE.md` 数据模型过时（那里只有体重和体脂）。复制 CLAUDE.md 过去时，我会同步改掉 `BodyMetric` 那一行。

---

## 三、功能范围

### 要做的（7 项）

| # | 功能 | 落地位置 |
|---|---|---|
| 1 | **动作库**：40 个预置动作（胸7/背7/腿9/肩6/手臂6/核心5）+ 可自建 | 设置 → 动作库 |
| 2 | **记训练**：选日期 → 加动作 → 记组（重量kg / 次数 / 可选RPE） | 训练 tab |
| 3 | **组间休息计时器**：默认 90 秒可调，结束有提示 | 训练 tab 内 |
| 4 | **历史记录**：按日期倒序，可展开细节 | 历史 tab |
| 5 | **统计**：单动作重量与容量趋势、周总容量、估算 1RM | 统计 tab |
| 6 | **训练模板**：一键套用（预置推日/拉日/腿日） | 设置 → 模板；训练页有"套用"按钮 |
| 7 | **身体数据**：身高、体重、体脂趋势 | 设置 → 身体数据 |

### 明确不做的

登录注册、云同步、需付费或 API Key 的服务、社交排行、AI 教练、动作识别、测试框架、后端、数据库。

---

## 四、数据模型

**先说人话**：数据模型就是"这个 App 要记住哪些东西，每样东西有哪些字段"。

这里有 6 样东西：

| 中文名 | 代号 | 记的是什么 |
|---|---|---|
| 动作 | `Exercise` | "杠铃卧推"这样的动作。属于哪个肌群、用什么器械、怎么做 |
| 组 | `SetEntry` | 你做的**一组**：多重、几次、累不累（RPE） |
| 训练 | `WorkoutSession` | **一次训练**：哪天做的、做了哪些动作的哪些组 |
| 模板 | `Template` | 一套预设方案，比如"推日 = 卧推4组 + 肩推3组…" |
| 身体数据 | `BodyMetric` | 某天量的身高/体重/体脂 |
| 设置 | `Settings` | 休息计时器默认多少秒、要不要显示 RPE |

**RPE** 是"自感用力程度"，1–10 分。8 分 = 还能再做 2 次。可以不填。

### 详细字段

**动作 `Exercise`**

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | 文字 | 不重复的编号，像身份证号。预置的写死成 `bb-bench-press` 这种 |
| `name` | 文字 | 中文名，如"杠铃卧推" |
| `muscleGroup` | 六选一 | 胸 / 背 / 腿 / 肩 / 手臂 / 核心 |
| `equipment` | 文字 | 器械，如"杠铃""哑铃""自重" |
| `isCustom` | 是/否 | 是你自己建的，还是预置的 |
| `note` | 文字（可空） | 一句话动作要领 |

**组 `SetEntry`** —— 一条 = 一组

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` / `exerciseId` | 文字 | 自己的编号 / 做的是哪个动作 |
| `weightKg` | 数字 | 重量。**0 是合法的**（引体向上、平板支撑），不是"没填" |
| `reps` | 数字 | 次数 |
| `rpe` | 数字（可空） | 1–10，可以有 7.5 这种小数 |
| `completedAt` | 时间 | 什么时候完成的 |

**训练 `WorkoutSession`**

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | 文字 | 编号 |
| `date` | `2026-09-23` | **本地日期**。这里有个大坑，见第七节 |
| `name` | 文字（可空） | 名字，通常来自模板，如"推日" |
| `entries` | 组的列表 | 这次训练做的所有组 |
| `note` / `durationSec` | 可空 | 备注 / 练了多久 |
| `startedAt` / `templateId` / `plannedItems` | 可空 | 开始时间 / 套用的模板 / 模板生成的计划清单 |

**模板 `Template`**：`{ id, name, items }`，`items` 是"动作 + 目标组数 + 目标次数"的列表。

**身体数据 `BodyMetric`**：`{ date, weightKg?, heightCm?, bodyFat? }` —— 一天一条，`date` 当身份证号。

**设置 `Settings`**：`{ restSec: 90, rpeEnabled: true }`

### 关于数据存储的 5 个设计决定

**① 40 个预置动作不进 localStorage，直接写在代码里。**
理由：省空间；改了能回滚；导出备份时更干净。
代价：**预置动作不能删也不能改**。想改就自建一个。

**② localStorage 里分 8 个格子存**，不是一个大格子。这样一个格子坏了不牵连其他，写历史时也不用重写动作库。

格子名（`nanofit:v1:` 是前缀）：
`meta` / `settings` / `exercises`（只存自建的）/ `sessions` / `templates` / `body-metrics` / `active-workout` / `profile`

**③ 正在进行的训练单独存一格（`active-workout`）。**
这是最重要的一个设计：**你每点一次 ✓，数据立刻落进手机**。练到一半锁屏、关浏览器、手机没电，重开还在。

**④ 一天算一次训练。**
同一天第二次进训练 tab，是**接着**那次练，不是新开一次。代价：一天练两回（早上+晚上）会合并成一条。

**⑤ 同一天量两次体重，后面覆盖前面**（不是记两条）。

---

## 五、页面结构

```
┌─────────────────────────────┐
│                             │
│      当前 tab 的页面内容      │   ← 上下滚动
│                             │
├─────────────────────────────┤
│  训练  │ 历史 │ 统计 │ 设置  │   ← 底部固定，4 个按钮
└─────────────────────────────┘
```

**手机全屏铺满；电脑上居中一条，最宽 480px，两边留白**（这样电脑上看着也不会像被拉伸的网页）。

| Tab | 里面有什么 |
|---|---|
| **训练** | 日期、动作列表、"0/4 组"进度、重量/次数/RPE 输入、大大的 ✓、休息计时器、套用模板按钮、结束训练 |
| **历史** | 按日期倒序的卡片列表，点开看每组的重量×次数 |
| **统计** | 本周总容量、最近 8 周容量柱状图、单动作的重量/容量折线图、1RM、身体数据趋势 |
| **设置** | → 动作库（40个+自建）<br>→ 训练模板（3个预置+自建）<br>→ 身体数据（记身高体重体脂）<br>→ 导出备份 / 导入恢复<br>→ 休息计时器默认秒数、要不要显示 RPE |

**配色**（你定的）：
页面底 `#121212`（深黑）· 卡片 `#1E1E1E`（浅一点的黑）· 主色 `#FF4D2E`（橙红）· 正文字白 · 次要文字灰

**手感要求**：所有能点的东西至少 44×44 像素（手指点得准）；重量和次数框点了弹**数字键盘**不是全键盘。

---

## 六、阶段划分

**每阶段做完我会停下来，告诉你在浏览器里能看到什么、能点什么。你验收通过了我才做下一阶段。每阶段结束都会 git commit 存档一次。**

### 阶段 1：初始化 + 空页面
把空文件夹变成一个能跑的项目。装好 Vite / React / TypeScript / Tailwind。
**文件数：新建 1 个，改 5 个，删 3 个**

### 阶段 2：动作库
40 个预置动作 + 搜索 + 按肌群筛选 + 自建动作。
**文件数：新建 6 个，改 2 个**

### 阶段 3：记训练 + 计时器（分 3 小步做，每步都停下来给你看）
- **3-1 能记一组**：选动作 → 填重量次数 → 点 ✓
- **3-2 休息计时器**：90 秒倒计时 + 结束提示音
- **3-3 防丢加固**：锁屏/关浏览器后数据不丢

**文件数：新建 7 个，改 3 个**

### 阶段 4：历史 + 模板 + 身体数据
历史列表、套用模板、记录身高体重体脂。
**文件数：新建 6 个，改 3 个**

### 阶段 5：统计图表
柱状图、折线图、1RM、周容量。
**文件数：新建 5 个，改 2 个**

### 阶段 6：PWA + README + 部署
装到手机桌面、断网能用、导出备份、写说明书、传上 GitHub。
**文件数：新建 6 个，改 3 个**

> 全项目约 **34 个源文件**。**没有任何一个阶段会一次生成超过 7 个文件**——这是你要求的节奏，我会守住。

---

## 七、技术坑与应对（这一节是我提前排的雷）

按"踩了会有多懵"排序：

**1. 手机上生成 ID 的功能会失效** 🔴
手机真机测试（`npm run dev -- --host`）时，地址是 `http://192.168.x.x`。浏览器规定：**只有 https 或 localhost 才算"安全环境"**，在这个地址下 `crypto.randomUUID()` 直接是 `undefined`，一用就崩。
**而这个 bug 在电脑上完全正常，一换手机就坏**——最难查的那种。
**应对**：ID 生成函数自带备用方案（时间戳+随机数）。

**2. 早上 8 点前记的训练会跑到"昨天"** 🔴
`new Date().toISOString()` 拿的是**格林威治时间**，比北京时间晚 8 小时。
实测：北京时间**凌晨 0 点到早上 8 点之间**，它给出的日期还是昨天 ——
早上 6 点半晨练完记录，训练会记到昨天去，历史排序和每周统计都会错位。
**应对**：日期工具自己拼 `年-月-日`，绝不用 `toISOString`。（阶段 3 已实施并实测验证）

**3. 手机切到后台时，计时器会走不准** 🔴
手机浏览器会把后台的定时器降频到 1 分钟一次，甚至冻住。如果按"每秒减 1"写，锁屏 10 分钟回来会发现还剩 80 秒。
**应对**：不记"还剩多少秒"，只记"**休息到几点结束**"。每次刷界面时用当前时间重新算一遍。这样哪怕定时器被冻住，算出来的数字永远是对的，切回前台立刻补上。

**4. iPhone 有个 7 天清理规则** 🔴
如果 7 天没跟这个网站互动过，Safari 会清掉它的本地数据。**"添加到主屏幕"装成 App 之后不受这条限制。**
**应对**：阶段 6 会强制引导你"添加到主屏幕"；并且**每周导出一次 JSON** 发到微信收藏。这是唯一可靠的备份。

**5. 休息结束的提示音，iPhone 上要特殊照顾** 🟡
浏览器规定：**不在你手指点击的那一瞬间，就不许出声**。所以要趁你点"开始休息"时（那正好是一次点击）把音响"解锁"。
震动只有安卓支持，iPhone 从来不支持网页震动——**这不是 bug，是苹果不给**。
**应对**：三重保险 = 界面变红闪大字（永远可用）+ 蜂鸣声 + 震动（能震就震）。

**5b. 切到别的 App 后，休息结束不会有任何提醒** 🔴（阶段 3 实测发现）
手机为了省电会**冻结后台页面的 JavaScript**。"到点了去响一声"这个动作是 JS 干的活，JS 冻住了就没人干。
这是**硬限制**：在"纯前端 + 不接后端"的前提下绕不过去 —— 想要后台提醒必须靠服务器推送。
**应对**：界面上明说"别切走"；休息期间保持屏幕常亮（`wakelock.ts`，浏览器要求 https，阶段 6 部署后生效）。
切回网页的那一瞬间会补上响铃 —— 但这里还有第二个陷阱：切到后台时音响会被系统**挂起**，
而唤醒它（`resume()`）是**异步**操作，不等它醒就发声会被系统丢掉。已修复并实测验证。

**6. 手机上输入框字号小于 16px，一点就整个页面放大** 🟡
在电脑上调不出来，只有 iPhone 真机上出现。
**应对**：输入框字号固定 ≥ 16px。

**7. iPhone 的数字键盘没有"完成"键** 🟡
键盘弹起来收不回去，会挡住下面的 ✓ 按钮。
**应对**：把 ✓ 放在输入框**紧挨着**的位置，让你不用先收键盘就能点。

**8. 底部按钮被 iPhone 的底部横条盖住** 🟡
**应对**：给底部留出"安全区"间距。

**9. 用 `type="number"` 做输入框会出各种怪事** 🟡
iPhone 上弹不出正确的键盘、小数点在部分输入法下变逗号、还会冒出上下箭头。
**应对**：统一封装成一个数字输入框组件，内部按文字处理、对外给数字。

**10. Service Worker 缓存极其顽固** 🟡
阶段 6 之后可能出现"我明明改了代码，打开还是老的"。
**应对**：缓存带版本号；README 里写清楚"急救步骤"。

**11. localStorage 大约只有 5MB，什么时候写满？** 🟢
算过账：一组约 150 字节，一周练 4 次约 15KB，**一年约 750KB，够用 6～7 年**。
**应对**：正常用写不满。但仍会做好"写满了"的友好提示，绝不偷偷吞掉错误。

**12. Windows 中文+空格路径的小坑** 🟢
`Nanofit工程` 这个名字会被脚手架自动改成 `nanofit-`（中文不许出现在项目名里），我会手动改回 `nanofit`。

**13. React 19 + recharts 图表空白？** 🟢
网上流传的这个问题是 **Preact** 的 bug，跟我们无关。recharts 3.10 官方支持 React 19。
唯一要注意的：图表外面的容器必须给个固定高度，否则算出来是 0 高度，图就看不见。

---

## 八、我还没定的 3 件事（都是阶段 6 的事，到时候再问你）

| 事项 | 两个选项 |
|---|---|
| PWA 怎么做 | **① 手写 Service Worker**（约 40 行，你能一行行读懂，出问题知道改哪）vs ② 装 `vite-plugin-pwa`（成熟方案，但多两个黑盒依赖，出问题只能求人） |
| 部署到哪 | **① GitHub Pages**（你的验收清单里要求有 GitHub 仓库，免费、和你写的"提交历史分段清晰"对得上）vs ② Netlify（更简单，拖一下就上传，但没有 git 提交历史） |
| App 图标 | 我用 Windows 自带工具生成一个"深黑底 + 橙红 N"的图标，或者你提供一张图 |

这三件到阶段 6 开工前我会再列一次选项给你拍板。

---

## 九、你确认后，我立刻做的事（阶段 1）

1. 把 `Nanofit设计\CLAUDE.md` 复制到 `Nanofit工程\CLAUDE.md`（并改掉 `BodyMetric` 那一行加上身高）
2. 把这份文档存成 `Nanofit工程\PLAN.md`
3. `git init` 建仓库 + 建 `.gitignore`
4. 跑 `npm create vite@latest . -- --template react-ts`，把项目名改成 `nanofit`
5. `npm install`，再装 `tailwindcss @tailwindcss/vite`
6. 按顺序改这几个文件，**每改一个告诉你一句它在干嘛**：
   - `vite.config.ts` —— 把 Tailwind 插进构建流程
   - `src/index.css` —— 定义你定的那三个颜色
   - `index.html` —— 中文网页、标题改成 NanoFIT、手机上全屏
   - `src/App.tsx` —— 4 个 tab 的骨架
   - `package.json` —— 项目名改干净
7. 删掉脚手架自带的样板垃圾文件
8. `npm run dev` 跑起来，把浏览器该看到什么告诉你
9. `git commit` 存档

---

## 十、验收方式

**你不需要懂代码，只需要打开浏览器看：**

- **阶段 1 做完**：一片深黑，中间写着"训练"，底部 4 个按钮（训练/历史/统计/设置），点哪个上面就变哪个，选中的是橙红色。按 F12 看控制台，**一条红字都没有**。
- **每个阶段**我都会给出这样一份"你现在应该看到什么"的清单，你照着点一遍就知道做没做完。
- **电脑上看还不够**：阶段 3 开始我会让你用手机连同一个 WiFi 测（终端跑 `npm run dev -- --host`，手机上打开显示的 IP 地址）。你的验收清单里"记一组到下一组 ≤5 秒（单手）"只有真机能测。
- **每阶段我都会跑一次 `npm run build`**（正式打包），确保不是"开发时能跑、打包就废"。

---

## 你的验收清单（我按这个收尾）

- [ ] 锁屏再开，刚记的数据还在
- [ ] 记一组到下一组 ≤5 秒（单手）
- [ ] 能翻出三个月前某动作的重量，对比现在
- [ ] 手机"添加到主屏幕"后像真 App，无地址栏
- [ ] 飞行模式能用
- [ ] 能导出 JSON
- [ ] GitHub 有仓库，提交历史分段清晰
- [ ] README 有三张截图 + 一段说明
